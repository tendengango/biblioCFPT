<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @cover.name %>
</p>

<p>
  <strong>Portrait:</strong>
  <%= link_to @cover.portrait.filename, @cover.portrait if @cover.portrait.attached? %>
</p>

<%= link_to 'Edit', edit_cover_path(@cover) %> |
<%= link_to 'Back', covers_path %>
